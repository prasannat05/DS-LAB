"""
ENGINEERED FEATURES - SHORT DESCRIPTIONS
"""

# ============================================================================
# INTERACTION FEATURES (Capture relationships between variables)
# ============================================================================

fico_income_ratio = fico_n / (revenue/10000 + 1)
# Credit score relative to income. Higher = good credit for income level.

dti_fico_product = (dti_n * fico_n) / 1000
# Debt burden × credit quality. Captures how they interact together.

loan_income_ratio = loan_amnt / (revenue + 1)
# Loan as % of income. Standard "Loan-to-Income" banking metric.

credit_capacity = (fico_n/850) * (revenue/max_revenue)
# Combined score: credit quality + income capacity (both normalized 0-1).

# ============================================================================
# RISK INDICATORS (Binary flags for high-risk conditions)
# ============================================================================

high_dti = (dti_n > 30).astype(int)
# Flag: 1 if debt-to-income > 30% (industry threshold for elevated risk).

low_fico = (fico_n < 650).astype(int)
# Flag: 1 if FICO < 650 (subprime borrower boundary).

experienced_high_income = (experience_c * (revenue > median)).astype(int)
# Flag: 1 if BOTH experienced worker AND above median income (lowest risk segment).

# ============================================================================
# POLYNOMIAL FEATURES (Capture non-linear relationships)
# ============================================================================

fico_squared = fico_n ** 2
# FICO². Captures accelerating benefit of higher credit scores.

dti_squared = dti_n ** 2
# DTI². Captures accelerating risk at higher debt levels.

revenue_log = np.log1p(revenue)
# Log(income). Handles skewness; extra income matters more when you're poor.

# ============================================================================
# WHY VALID & LEGAL
# ============================================================================

"""
✓ All derived from existing data (no new information)
✓ Objective financial calculations (no bias)
✓ Standard banking/econometric techniques
✓ No protected class attributes (race, gender, religion, age)
✓ Compliant with ECOA, FCRA, Dodd-Frank regulations
✓ Used by major banks and Federal Reserve
"""
